module.exports = interface = {
    data: {
        base: String,
        api: String,
        server: String,
        riot: String,
    },
    url: {
        playerData: String,
        partyService: String,
        sharedData: String,
    },
    riot: {
        api: String,
        esports: String,
        server: String,
    }
}